# Second Note

Backlink target for [[Welcome]]. Also links into a folder: [[Projects/Alpha]].

Some `inline code` and a block:

```python
def hello():
    return "world"
```

Math: $e^{i\pi} + 1 = 0$
