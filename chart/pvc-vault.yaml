apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault
  namespace: livesync
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 5Gi
