apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: static
  namespace: livesync
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 5Gi
