# Placeholder — replace values or manage via ExternalSecrets / SealedSecrets.
# Values below are base64-encoded placeholders (decode: "changeme" / "sk-ant-xxxxx").
apiVersion: v1
kind: Secret
metadata:
  name: livesync-secrets
  namespace: livesync
type: Opaque
data:
  COUCHDB_PASSWORD: Y2hhbmdlbWU=          # changeme
  ANTHROPIC_API_KEY: c2stYW50LXh4eHh4     # sk-ant-xxxxx
  JWT_SIGNING_KEY: ZGV2LXNlY3JldC1rZXk=   # dev-secret-key

---

# Okta OIDC credentials for ALB auth — referenced by ingress.yaml.
# Replace with real values from your Okta OIDC app registration.
apiVersion: v1
kind: Secret
metadata:
  name: okta-oidc-secret
  namespace: livesync
type: Opaque
data:
  clientID: PLACEHOLDER_BASE64
  clientSecret: PLACEHOLDER_BASE64
